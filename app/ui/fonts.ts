import { Inter, Lusitana, Kanit } from 'next/font/google';
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const kanit = Kanit({
  subsets: ['latin'],
  weight: ['400', '800', '500'],
});
